# Upstream configuration for Flask application (Managed by Blue/Green deploy)
# Switches dynamically between Blue (127.0.0.1:5000) and Green (127.0.0.1:5002)
upstream flask_web_backend {
    server 127.0.0.1:5000;
    keepalive 32;
}
